# Pool Ladder Website Environment Variables

# JWT Secret Key - Change this to a secure random string in production
# Generate a secure key: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Admin Password Hash - Format: salt:hash
# To generate: node scripts/generate-password.js [your-password]
# For default setup (password: admin123), leave commented out
# ADMIN_PASSWORD_HASH=salt:hash

# Production Settings
NODE_ENV=production